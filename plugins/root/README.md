# root

## Useful commands
```bash
# Lock root account
sudo passwd -l root

# Unlock root account
sudo passwd -u root

# Check if root is locked
# Status L = locked
# Status P = usable password
sudo passwd -S root
```
